<% content_for :title, "Comments" %>

<h1>Comments</h1>

<div id="comments">
  <% @comments.each do |comment| %>
    <%= render "com", comment: comment %>
  <% end %>
</div>

<%= link_to "New comment", new_comment_path %>
